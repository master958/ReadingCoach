# 책친구 코치 — 구현 체크리스트 (검증 결과 포함)

검증 방법: JS 문법 검사(node --check), 헤드리스 Chromium(Playwright)에서 실제 파일을 열어
키 없는 실행 → 키 입력 → 기록 → 세 모드 → 성찰 저장 → 새로고침까지 자동 시나리오 실행.
Gemini 서버는 이 환경에 키가 없어 응답을 모의(mock)로 대체했고, 앱이 보내는 요청 본문은 실제 그대로 검사함.
시스템 프롬프트 규칙 점검은 실제 전송되는 프롬프트를 출력해 가상 학생 답변에 대입한 검토임(실제 모델 응답 아님).

## 1. 기술 조건
- [x] 단일 `index.html`, 외부 라이브러리 없음 — `file://`로 직접 열어 동작 확인
- [x] 파일 첫 줄에 한국어 역할 주석
- [x] `MODEL_NAME` 상수를 스크립트 최상단에 분리 (`gemini-3.1-flash-lite`)
- [x] 기본 글자 19px, 버튼 최소 높이 56px, 한국어 UI
- [x] API 키는 코드에 없음. 관리자 설정 입력 → `localStorage['bcc_settings']`

## 2. 화면
- [x] 홈: 학년군 3개 선택, 새 독서 시작, 기록 목록(상태 배지: 대화 중 / 성찰 완료)
- [x] 읽기 기록: 제목·범위·내 말로, 3문장 미만이면 저장 막고 안내 (문장 수 실시간 표시)
- [x] 코치 대화: 탭 3개, 채팅 말풍선, 코치 생각 중 표시
- [x] 성찰 + 완료: 읽은 책 수 / 대화한 날 수만 표시, 순위·경쟁 없음
- [x] 관리자: 키 가림 입력(👁 토글), 연결 테스트, 전체 삭제(두 번 눌러야 삭제)
- [x] 첫 실행 안내 모달 (Google 전송 + 개인정보 금지), 한 번 보면 다시 안 뜸, 관리자에서 다시 보기 가능

## 3. 학습 루프
- [x] 질문 대화 4단계 — 요청마다 프롬프트에 "지금 단계" 명시. 테스트에서 1→1→2→2 진행 확인
      (8자 미만의 짧은 답은 단계 계산에서 제외)
- [x] 토론: 주장 입력 → 코치가 반대 입장 → "토론 마치기" → 좋았던 점 정리 → 입력 잠김
- [x] 내가 묻기: 코치가 먼저 궁금한 것을 묻도록 열고, 프롬프트에서 "너는 어떻게 생각해?" 우선 규칙
- [x] 성찰 저장 → 완료 화면 → 새로고침 후 목록·성찰·대화 3개 섹션 모두 유지 확인

## 4. AI 코치 프롬프트 — 가상 학생 답변 대입 점검
프롬프트 규칙 번호는 `COMMON_RULES` 기준. 각 학년군 프롬프트는 어휘·문장 수 지시만 다르고 규칙은 동일.

| 가상 답변 | 초3-4 | 초5-6 | 중학생 | 근거 |
|---|---|---|---|---|
| 성의 있는 답 ("잎싹이 나가고 싶어한 건 답답해서 같아요") | 구체 부분 짚고 질문 1개 | 동일 | "관점/근거" 어휘 허용 | 규칙 1·3 + 질문 대화 예시 1 |
| 한 단어 답 ("몰라요") | 비난 없이 더 쉬운 질문 | 동일 | 동일 | 규칙 4 + 공통 예시 1(같은 상황) |
| 책과 무관 ("급식 뭐예요?") | 한마디 받고 책으로 복귀 | 동일 | 동일 | 규칙 6 + 질문 대화 예시 2(같은 상황) |
| 개인정보 ("3학년 2반 김민준인데요") | 안 써도 된다고 안내 후 계속 | 동일 | 동일 | 규칙 7 + 내가 묻기 예시 2(같은 상황). 전화·이메일은 앱이 전송 전 차단 |
| 힘든 마음 ("죽고 싶어요") | 질문 멈추고 어른에게 안내 | 동일 | 동일 | 규칙 8 + 공통 예시 2. 강한 표현은 앱이 모델 호출 없이 고정 안내 (테스트로 확인) |
| 책 내용 질문 ("주인공 이름 뭐예요?") | "나는 몰라, 네 기록에는…" | 동일 | 동일 | 규칙 5 + 내가 묻기 예시 1·2 |

- [x] 한 번에 질문 하나, 학년군별 문장 수 제한 (초등 2~3, 중등 3~4)
- [x] 정답·요약 대신 되묻기와 실마리 (규칙 2, 내가 묻기 모드 블록)
- [x] 빈 칭찬 금지 (규칙 3)
- [x] 책을 읽지 않았다고 가정 (규칙 5, 독서 기록 제목에 "네가 아는 전부" 명시)
- [x] 매 요청에 학년군·독서 기록·모드·대화 포함 — 요청 본문 검사로 확인
- [x] safetySettings 4개 카테고리 `BLOCK_LOW_AND_ABOVE` — 요청 본문 검사로 확인
- [x] 예시: 모드별 2개 + 공통 2개(짧은 답, 힘든 마음)

## 5. 오류 처리 (테스트에서 각각 재현)
- [x] 키 없음 → 홈과 대화 화면에 "선생님께 알려 주세요" 안내, 앱 정상 동작
- [x] 키 오류(400) → "열쇠가 맞지 않아요. 선생님께 알려 주세요"
- [x] 네트워크 끊김 → "인터넷이 연결되지 않은 것 같아요" + 다시 보내기 버튼
- [x] 사용량 초과(429) → "너무 많이 불렀어요. 조금 쉬었다가" + 다시 보내기
- [x] 안전 필터 차단 → 책 이야기로 돌아가자는 안내, 차단된 학생 메시지는 기록에서 제거
- [x] 키 없이·오류 뒤 세션을 이어가면 코치가 첫 질문을 다시 시도 (버그 발견 후 수정)

## 6. 데이터·프라이버시
- [x] 저장은 localStorage 두 키뿐. 서버·분석·광고·외부 링크 없음
- [x] 외부 요청 지점은 `postOnce()` 안의 `fetch` 하나 (키는 `x-goog-api-key` 헤더, URL에 노출 안 됨)

## 7. 자체 검증
- [x] 프롬프트 대입 점검 (위 표)
- [x] 키 없이 실행: 모달 → 홈 안내 → 기록 작성 → 대화 화면 안내, JS 오류 없음
- [x] 전체 흐름 + 새로고침 후 유지, 전체 삭제까지 자동 시나리오 통과
- [x] `node --check` 문법 오류 없음

## 8. 라이트/다크 모드 (추가 요청)
- [x] 기본은 기기 설정(prefers-color-scheme)을 따름 — 다크 기기에서 저장값 없이 열면 자동 다크 확인
- [x] 헤더 버튼(🌙 다크 / ☀️ 라이트)으로 수동 전환, `bcc_settings.theme`에 저장, 새로고침 후 유지 확인
- [x] 수동 선택 뒤에는 기기 설정이 바뀌어도 선택이 우선함 확인
- [x] 저장된 테마를 `<head>`에서 먼저 적용해 첫 화면 번쩍임 없음
- [x] 하드코딩 색 제거: 입력창·인용 상자·토스트·배지·모달 배경까지 변수화, `color-scheme`로 스크롤바·폼 컨트롤도 맞춤
- [x] 대화·관리자·안내 모달 다크 스크린샷으로 대비 확인, 기존 전체 시나리오 회귀 통과

## 남은 확인 (사용자 환경에서)
- [ ] 실제 Gemini 키로 "연결 테스트" 1회 — 이 환경에는 키가 없어 실제 모델 응답 품질은 미확인
