# 책친구 코치 — 결정 사항과 이유

## Gemini API 확인 결과 (2026-09-18)
- 이 작업 환경에서 `ai.google.dev`는 네트워크 정책으로 차단됨. 대신 Google 공식 GitHub
  (google-gemini/cookbook, googleapis/python-genai)와 공식 deprecation 문서의 검색 결과로 확인함.
- 모델 ID `gemini-3.1-flash-lite`: 2026-05-07 정식 출시, 2027-05-07 종료 예정. 요청한 ID 그대로 사용.
  (더 최신인 `gemini-3.5-flash-lite`가 있으나 사용자 지시대로 임의 변경하지 않음. 교체는 `MODEL_NAME` 한 줄.)
- 엔드포인트: `POST https://generativelanguage.googleapis.com/v1beta/models/{MODEL}:generateContent`
- 인증: `x-goog-api-key` 헤더. (쿼리스트링 `?key=`도 되지만 URL에 키가 남지 않도록 헤더 사용)
- 본문 필드: `system_instruction.parts[].text`, `contents[].{role,parts[].text}`,
  `safetySettings[].{category,threshold}`, `generationConfig`
- 안전 설정: 가장 엄격한 임계값은 `BLOCK_LOW_AND_ABOVE`. 적용 카테고리 4개
  (HARASSMENT, HATE_SPEECH, SEXUALLY_EXPLICIT, DANGEROUS_CONTENT).
  `HARM_CATEGORY_CIVIC_INTEGRITY`는 공식 SDK에 deprecated로 표시되어 보내지 않음.
- Gemini 3 계열은 기본으로 "생각(thinking)"을 함. 학생 대화는 응답 속도가 중요하므로
  `generationConfig.thinkingConfig.thinkingLevel = "LOW"`를 보냄. 만약 이 필드로 400이 나면
  thinkingConfig 없이 1회 재시도함 (모델이 필드를 거부해도 앱이 멈추지 않도록).

## 설계 판단 (요구를 목적에 비추어 조정한 부분)
1. **질문 대화의 깊이 단계를 앱이 추적한다.** 가벼운 모델은 "점차 깊어져라"는 지시만으로는
   단계 관리가 불안정하다. 앱이 학생 답변 수를 세어 현재 단계(사실→추론→비판→적용)를
   매 요청마다 시스템 프롬프트에 명시한다. 단계 이동은 학생 답변 2회마다.
2. **성찰 화면에서 학생이 처음 쓴 "내 말로" 글을 옆에 보여 준다.** 성공 기준이
   "처음보다 더 깊은 생각을 자기 말로 쓰는가"이므로, 처음 글과 비교하면서 쓰게 한다.
   새 기능이 아니라 기존 성찰 화면의 배치 판단.
3. **대화 내용 전송 범위는 현재 모드의 대화만.** 세 모드는 목적이 달라 다른 모드의 대화를
   섞으면 가벼운 모델이 역할을 혼동하기 쉽고 토큰도 늘어난다. 독서 기록은 항상 포함.
4. **대화 첫 질문은 코치가 먼저 연다.** 모드에 들어가면 화면에 보이지 않는 시작 메시지를
   보내 코치가 첫 질문을 하도록 한다. 학생이 빈 화면 앞에서 막히지 않게 하기 위함.
5. **토론 종료는 명시적 버튼("토론 마치기")으로.** 끝을 모델이 판단하게 두면 불안정하다.
   버튼을 누르면 반대 역할을 내려놓고 학생 주장의 좋았던 점을 구체적으로 짚는 요청을 보낸다.
6. **각 모드 8번째 학생 답변부터는 "마무리 단계" 힌트를 보낸다.** 1회 10~15분 루프를 지키기
   위해 코치가 마지막 질문 하나로 정리하게 유도. 학생은 언제든 "성찰 쓰러 가기"로 넘어갈 수 있다.
7. **시스템 프롬프트 구성**: 공통 규칙(짧게) + 학년군 블록 + 모드 블록 + 모드별 예시 2~3개 +
   독서 기록. 규칙은 번호 목록으로, 예시는 "학생 → 코치" 형식.
8. **위험 신호는 모델 규칙에만 의존하지 않는다.** 앱에서도 간단한 키워드(죽고 싶, 자해, 때려,
   무서워 등)를 감지하면 코치 호출 전에 고정 안내문을 먼저 보여 준다. 모델이 놓쳐도 안내가 나가게.
   개인정보(전화번호·이메일 패턴)는 전송 전에 학생에게 지우라고 안내하고 전송하지 않는다.

## 불명확한 점 → 채택한 가정 (사용자가 실시간 답변 불가하여 가정으로 진행)
- 학년군은 홈에서 고르고 전역 설정으로 저장. 각 독서 기록에도 당시 학년군을 저장.
- "3문장 이상" 검사: 마침표·물음표·느낌표·줄바꿈 기준으로 나눠 3개 이상. 초등학생은
  마침표를 빼먹기 쉬워 줄바꿈도 문장 구분으로 인정.
- 홈의 기록 목록에서 항목을 누르면 상세(기록·성찰·대화)를 볼 수 있고, 성찰을 아직 안 쓴
  기록은 이어서 대화할 수 있다.
- "읽은 책 수"는 서로 다른 책 제목 수, "대화한 날 수"는 성찰을 저장한 날짜 수(중복 제외).
- 관리자 설정 진입에 비밀번호는 두지 않음 (요청에 없음). 전체 삭제는 2단계 확인.
- 응답 최대 길이: maxOutputTokens 400. 초등은 2~3문장, 중학생은 3~4문장으로 프롬프트에서 제한.
- temperature 0.7.

## 저장 구조 (localStorage)
- `bcc_settings`: `{ apiKey, gradeBand, noticeSeen }`
- `bcc_sessions`: 배열. 각 항목
  `{ id, createdAt, gradeBand, title, range, summary, chats: {question:[], debate:[], ask:[]},
     debateClaim, debateEnded, reflection, completedAt }`
  chat 메시지: `{ role: 'user'|'model', text, hidden?: true }` (hidden은 앱이 보낸 시작/종료 지시)

## 구현 중 추가 결정
- 질문 대화 단계 계산에서 8자 미만 답변은 제외. "몰라요" 두 번으로 단계가 올라가지 않도록.
  (마무리 힌트 기준인 8회는 모든 답변을 셈 — 시간 제한 목적이므로)
- 키 없음·네트워크 오류로 코치 응답이 없는 상태에서 세션을 이어가면(마지막 메시지가 학생/앱 차례)
  자동으로 코치를 다시 부른다. 처음 구현에서는 시작 지시가 이미 있다는 이유로 건너뛰어 빈 화면이 됐음.
- 토론 시작 시 "내 주장: …"을 학생 말풍선으로 남겨 대화 기록만 봐도 주장이 보이게 함.
- 안전 필터로 차단된 학생 메시지는 대화 기록에서 지운다. 남겨 두면 다음 요청도 계속 막히기 때문.
- Gemini 3 계열은 응답에 thought 파트가 섞일 수 있어 `part.thought`가 참인 조각은 버린다.
